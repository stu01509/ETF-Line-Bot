name: Health Check Self Hosted

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  cron:
    runs-on: self-hosted

    steps:
      - name: Health Check
        run: curl https://tw-etf-line-bot.azurewebsites.net
